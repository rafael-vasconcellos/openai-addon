{
  "name": "gemini-addon",
  "title": "Gemini",
  "description": "Google's gemini support for Translator++.",
  "author": "",
  "version": "1.0.0",
  "main": "main.js",
  "autoload": false,
  "load": [
    "main.js"
  ],
  "scripts": {
    "build": "tsc && cp package.json ./dist && node esbuild.config.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "type": "module",
  "keywords": [],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.9.1",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0"
  }
}