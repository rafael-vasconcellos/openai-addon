from setuptools import setup, find_packages

setup(
    name="g4f_inference",
    version="1.0",
    packages=["g4f_inference"],
    #install_requires=[]
)

"""
entry_points={
        "console_scripts": [
            "g4f_inference=g4f_inference.__main__:main",  # Substitua pelo ponto de entrada correto
        ],
    },
"""